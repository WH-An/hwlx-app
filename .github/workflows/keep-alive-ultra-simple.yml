name: Keep Alive Ultra Simple

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping website
        run: |
          echo "开始保活..."
          curl -s https://hai-wai-liu-xue.onrender.com
          echo "保活完成"
