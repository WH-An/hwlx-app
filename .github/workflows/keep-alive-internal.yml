name: Keep Alive Internal

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Internal check
        run: |
          echo "🔄 内部保活检查"
          echo "📅 $(date)"
          echo "✅ 检查完成"
          echo "⏰ 下次检查: 5分钟后"
