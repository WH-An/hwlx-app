name: Keep Alive Actions

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Use actions/http-request
        uses: actions/http-request@v1
        with:
          url: 'https://hai-wai-liu-xue.onrender.com'
          method: 'GET'
          timeout: 30
