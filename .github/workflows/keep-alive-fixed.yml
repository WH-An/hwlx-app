name: Keep Alive Fixed

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Check website status
        run: |
          echo "=== 保活检查开始 ==="
          echo "时间: $(date)"
          echo "目标: https://hai-wai-liu-xue.onrender.com"
          
          # 简单的curl请求
          curl -I https://hai-wai-liu-xue.onrender.com || echo "请求失败"
          
          echo "=== 保活检查完成 ==="
